/**
 * @file constants.c
 * @brief Definitions for any non-macro constants (currently placeholder).
 *
 * All constants in EraseCure are defined as macros in constants.h.
 * This file exists for build-system completeness and can host
 * any future non-macro constant definitions.
 */
#include "common/constants.h"

/* No runtime definitions required at this time. */
